<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">

	<title>MADEER </title>

	<link href="{{ url_for('static', filename='bootswatch/darkly/bootstrap.min.css') }}" rel="stylesheet">
  <link href="{{ url_for('static', filename='css/styles.css') }}" rel="stylesheet">
  {% block extra_css %}
  {% endblock %}
</head>

<body>

	<div class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
        	<img src="{{ url_for('static', filename='img/seal.png') }}" width="50" style="margin-top:4px;float:left;"/>
          <a href="{{ url_for('index')}}" class="navbar-brand">MADEER </a> 
          <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navbar-main">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div class="navbar-collapse collapse" id="navbar-main">

					<ul class="nav navbar-nav navbar-right">
						{% if g.user.is_authenticated() %}
            <li class="navbar-text"><span class="muted">{{ current_user.email }}</span></li>
              {% if g.user.is_admin() %}
            <li><a href="{{ url_for('index') }}">Admin</a></li>
	    <li><a href="admin_meta" >Meta</a></li>
              {% endif %}
						<li><a href="{{ url_for('logout') }}">Cerrar sesión</a></li>
						{% else %}
						<li><a href="{{ url_for('create_acct') }}">Crear una cuenta</a></li>
						<li><a href="{{ url_for('login') }}">Ingresar</a></li>
						{% endif %}
					</ul>

        </div>
      </div>
    </div>

    <div class="container">
      <div class="row" style="margin-top:12%;">
        <div class="col-lg-12">
					{% block content %}
					{% endblock %}
				</div>
			</div>
		</div>

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
  <script src="{{ url_for('static', filename='bootstrap/dist/js/bootstrap.min.js') }}"></script>
  {% block extra_js %}
  {% endblock %}
</body>
</html>
